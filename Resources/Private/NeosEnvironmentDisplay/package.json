{
  "version": "1.0.0",
  "description": "Configure custom display-names for environments in the neos backend",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ../../Public/NeosEnvironmentDisplay && node esbuild.js",
    "watch": "node esbuild.js --watch"
  },
  "dependencies": {
    "@neos-project/neos-ui-extensibility": "~8.3.7",
    "@neos-project/react-ui-components": "~8.3.7",
    "esbuild": "~0.17.19"
  }
}
